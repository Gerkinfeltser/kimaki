---
'kimaki': minor
---

Add a **Run now** button to `/tasks` so you can fire a planned scheduled task immediately.

Uses the same claim → Discord send → finalize path as the normal task runner, so one-time and cron jobs land in the same channel/thread with the same start markers.

| Action | Button |
| --- | --- |
| Run early | **Run now** (planned tasks) |
| Remove | **Delete** (planned or running) |
